<!-- Generated by AI Rulesmith — do not edit manually -->
<!-- Target: Claude Code | Type: standard -->
<!-- Rules: ai-behavior/minimal-diffs, ai-behavior/confirm-destructive-actions, workflow/understand-before-changing, workflow/verify-before-completing, workflow/incremental-changes, code-style/strict-typescript, code-style/small-focused-functions, code-style/early-returns-and-guards, code-style/comments-explain-why-not-what, testing/never-weaken-tests, testing/isolated-unit-tests, security/validate-external-input, git/conventional-commits, git/feature-branch-workflow, memory/consult-memory-before-work, memory/save-discoveries-to-memory, life-manager/manage-tasks, life-manager/track-project-progress, project/twb-parsing-context, project/project-conventions, project/tech-stack, project/verification-steps -->


# Minimal Diffs

- Change only what was requested — nothing more
- Don't refactor surrounding code during a bug fix
- Don't add docstrings/comments/types to unchanged code
- Don't rename variables or reorganize imports in files you're not modifying
- Each commit should be reviewable in isolation

---

# Confirm Destructive Actions

- Confirm before deleting files, overwriting work, or running destructive commands (force push, reset --hard)
- Don't make changes beyond what was requested — a bug fix doesn't need surrounding code refactored
- If the task is larger than expected, pause and discuss before continuing
- Investigate unexpected state (unfamiliar files, branches, configs) before modifying it
- If an action cannot be undone, say so explicitly when asking for confirmation

---

# Understand Before Changing

- Read and understand existing code before modifying it
- Look for existing functions, utilities, and patterns to reuse
- Identify all affected files and assess the blast radius
- Outline implementation steps in dependency order
- Get user alignment on approach before starting
- If discoveries during implementation change the plan, pause and reassess

---

# Verify Before Completing

- Run linter, type checker, and tests after every change
- Never declare a task done until verification passes
- If a check fails, fix the issue — don't skip the check
- Automate checks with pre-commit hooks or CI gates
- Build must succeed before merging

---

# Incremental Changes

- Break large tasks into small, shippable increments
- Each increment should compile, pass tests, and be reviewable
- Commit after each meaningful chunk — not one giant commit at the end
- If the approach changes mid-task, commit current progress first
- Smaller changes are easier to review and safer to revert

---

# Strict TypeScript

- Use strict TypeScript config (`"strict": true`)
- Explicit return types on all public/exported functions
- Prefer `interface` for object shapes; `type` for unions/intersections
- Never use `any` — prefer `unknown` with type guards
- `const` over `let`; never `var`
- Template literals over string concatenation
- `??` over `||` for defaults; `?.` for null checks
- `import type` for type-only imports
- Named exports over default exports

---

# Small Focused Functions

- Functions do one thing; keep them under 50 lines
- Files under 400 lines; hard limit ~800 lines
- Max nesting depth: 3-4 levels
- Extract only when reused, enables testing, or drastically improves readability
- If a function needs a comment explaining what it does, it's too complex — break it up

---

# Early Returns and Guards

- Check preconditions at the top of functions: null, invalid state, out-of-range values
- Return early or throw immediately on invalid input — don't nest the happy path inside if-else
- Each guard clause handles one condition and exits (return/throw) — keeps logic flat
- Reduces indentation depth — the main logic runs at the shallowest nesting level
- Eliminates `else` blocks by handling the negative case first and returning

---

# Comments Explain Why Not What

- Don't comment obvious code — good names and structure are self-documenting
- Do comment non-obvious design decisions (why, not what)
- Comment when processing order matters or a fall-through is intentional
- Use TODO/FIXME sparingly and with context (what needs doing and why)
- Delete commented-out code — version control is the history

---

# Never Weaken Tests

- When a test fails after a code change, the test is probably right — fix the code
- Only modify tests if the change was intended to affect the tested behavior
- Never weaken assertions to make failing tests pass
- Never delete a failing test unless the feature it covers was intentionally removed
- If you're unsure whether a test should change, ask before modifying it

---

# Isolated Unit Tests

- Arrange-Act-Assert pattern
- One logical assertion per test
- Test names describe behavior: `[unit] [scenario] [expected result]`
- Mock external deps; each test independent; no shared state leaks
- Test boundary values, edge cases, error paths
- Test public API surface, not internals
- Keep fixtures minimal — only data relevant to the test

---

# Validate External Input

- Validate all external input at system boundaries (API params, file paths, user input, webhook payloads)
- Validate file paths to prevent path traversal — reject `..` segments, resolve to expected base directory
- Never execute user-provided strings as code or shell commands without sanitization
- Whitelist acceptable values where possible rather than blacklisting dangerous ones
- Validate early, before the data is passed deeper into the system

---

# Conventional Commits

- Format: `type(scope): description`
- Types: feat, fix, docs, style, refactor, test, chore
- Subject line: imperative mood, under 72 chars, no trailing period
- Be specific: "fix null check in user validation" not "fix bug"
- Commit during work with meaningful chunks — not one giant commit at the end
- Each commit should leave the codebase in a working state

---

# Feature Branch Workflow

- Create a feature branch before making any changes
- Branch from the main branch (main/master)
- Make meaningful commits during work
- Run tests, type checker, and build before pushing
- Push the branch and create a pull request — never merge directly into main
- Delete the branch after the PR is merged

---

# Consult Memory Before Work

- Before starting any development work, read the auto-memory files in the project's memory directory
- Search OpenBrain (via MCP) for thoughts related to the current task — past solutions, architectural decisions, and known pitfalls
- Look for relevant past decisions, patterns, gotchas, and solutions that apply to the current task
- If the task involves infrastructure, deployment, or integration — check memory and OpenBrain for known pitfalls and working configurations
- Do not re-discover things that are already documented — use what's there
- If memory contains outdated or incorrect information, flag it and update it

---

# Save Discoveries to Memory

- After resolving a non-trivial problem (debugging, configuration, integration), save the key findings to the project's memory files and as a thought in OpenBrain (via MCP)
- Include: what went wrong, why, and the working solution — not just the fix but the reasoning
- After completing a feature or significant chunk of work, update memory with architectural decisions and patterns used
- Save information that would save time if encountered again: correct config values, API quirks, dependency gotchas
- Keep memory entries concise and scannable — bullet points over paragraphs
- Do not duplicate information already in CLAUDE.md or other project docs — memory is for hard-won knowledge not covered elsewhere
- Tag OpenBrain thoughts with relevant topics for easy retrieval via semantic search

---

# Manage Tasks in Life Manager

- Use the life_manager MCP tools to manage tasks for the **{{project_name}}** project
- At the start of a session, list existing tasks to understand what's in progress and what's pending
- When the user requests work, check if a matching task exists — if so, mark it `in_progress`; if not, create one
- When work is complete, mark the task as `done`
- If you discover additional work during implementation, create new tasks for it
- Keep task names short and actionable (e.g., "Add marriage system", "Fix energy bug")

---

# Track Project Progress

- After completing a task or a meaningful chunk of work, update the **{{project_name}}** project in life_manager
- Use the life_manager MCP tools to list tasks, mark them done, and add newly discovered tasks
- Search for the project by name if you need the ID

---

# TWB Parsing Domain Context

This project parses Tableau Workbook (.twb) XML files to extract field dependencies and visualize them as a graph.

## Key Domain Documents

Before modifying any parsing logic, read these files thoroughly:
- `AIRulesForParsingTWBFiles.md` — parsing rules, field types, formula parsing order
- `TWBStructureDocumentation.md` — XML structure reference with examples

## Field Types

There are three types of fields:
1. **Parameters** — found only in the datasource named "Parameters"
2. **Calculations** — columns with a `<calculation>` child tag, found in any datasource
3. **Data source fields** — defined across multiple locations (relations, column mappings, metadata records, direct columns)

## Formula Parsing Order (Critical)

When parsing calculation formulas for field references:
1. Remove all comments (single-line `//` and multi-line `/* */`)
2. Remove all quoted strings (single and double quotes, with escape support)
3. Parse remaining text for field references (`[Field Name]` or `[Datasource].[Field Name]`)

## Data Source Field Processing Order (Critical)

1. Read from relations
2. Apply column mappings
3. Apply metadata records
4. Apply direct column modifications (user changes)

## Exceptions

- Ignore datasources where `relation type="pivot"`
- Only handle single-level relations for now

## XML Parser

The project uses `fast-xml-parser` for XML parsing. Do not switch to another parser without discussion.

---

# Project Conventions

## File Naming

- **React components**: PascalCase, `.tsx` extension (e.g., `FieldDetails.tsx`)
- **Utilities**: camelCase, `.ts` extension (e.g., `twbParser.ts`)
- **Type definitions**: camelCase, `.types.ts` extension (e.g., `app.types.ts`)
- **Unit tests**: camelCase `.test.ts` or PascalCase `.test.tsx` for components, in `__tests__/unit/`
- **E2E tests**: camelCase `.spec.ts`, in `__tests__/e2e/`

## Directory Structure

```
src/
  components/    — React components
  utils/         — Utility functions and helpers
  types/         — TypeScript type definitions
  hooks/         — React hooks
  data/          — Static data
  theme/         — Theme/styling
__tests__/
  unit/          — Jest unit tests
  e2e/           — Playwright e2e tests
```

## Testing Selectors

Use `data-testid` attributes for E2E test selectors (e.g., `data-testid="submit-button"`).

## Code Formatting

Prettier is configured (see `.prettierrc`). Do not change formatting rules.

---

# Tech Stack

## Core

- **React 18** with functional components and hooks
- **TypeScript** with strict mode
- **Vite** for dev server and builds
- **MUI (Material UI) v6** for UI components with Emotion for styling

## Key Libraries

- **ReactFlow v11** — graph visualization of field dependencies
- **dagre** — graph layout algorithm
- **fast-xml-parser** — XML parsing of .twb files
- **lodash** — utility functions
- **react-router-dom v6** — routing

## Testing

- **Jest** with `ts-jest` — unit tests
- **Playwright** — e2e tests
- **@testing-library/react** — component testing

## Commands

- `npm run dev` — start dev server
- `npm run build` — typecheck + build
- `npm run typecheck` — TypeScript check only
- `npm test` — run all tests (unit + e2e)
- `npm run test:unit` — unit tests only
- `npm run test:unit:single <path>` — single test in watch mode
- `npm run test:e2e` — e2e tests only

---

# Verification Steps

After making changes, run these checks before declaring work complete:

1. **TypeScript**: `npm run typecheck`
2. **Unit tests**: `npm run test:unit`
3. **E2E tests** (if UI changed): `npm run test:e2e`

If only modifying parser/transformer logic, unit tests are sufficient.
If modifying components or UI behavior, run both unit and e2e tests.

---

# Changelog

The project has an in-app changelog at `src/data/changelog.ts`. When making user-facing changes (features, fixes, UI improvements), add a new entry at the **top** of the `changelogData` array before committing. Follow the existing format:

```ts
{
  version: "<next version>",
  date: "YYYY-MM-DD",
  type: "feature" | "fix" | "improvement",
  changes: ["Description of change"],
}
```

Bump the version following semver: patch for fixes, minor for features, major for breaking changes.
